{% extends 'base.html' %}
{% block list %}
<div class="container">
<div class="row row-offcanvas row-offcanvas-right">
  <div class="col-xs-11 col-sm-2 sidebar-offcanvas" id="sidebar" role="navigation">
    <div class="row">
      <div class="col-md-12">
                <!--Sidebar content-->
                <div class="sidebar-menu">
         <!-- <div class="menu-first" ><a href="#userMenu" data-toggle="collapse"><i class="glyphicon glyphicon-th
"></i> 证金管理</a></div> -->
          <ul id="userMenu" class="nav nav-list navbar-collapse.in menu-second">
            <li><a href="/user/"><i class="glyphicon glyphicon-send"></i> 用户列表</a></li>
            <li><a href="/addUser/"><i class="glyphicon glyphicon-send"></i> 增加用户</a></li>
          </ul>

        </div>
      </div>
    </div>
  </div>

<!---->
{% endblock %}

{% block content %}
    <form method="post" action="">
        {% if info %}
            <p class="alert alert-success">
                {{ info }}
            </p>
        {% endif %}
        <table class="table table-striped table-hover table-condensed">
        <thead>
            <tr>
                <th><input onclick="selectAll()" type="checkbox"  name="select_all" style="select_all" id="select_all"/></th>
                <th>UserID</th>
                <th>用户名</th>
                <th>姓名</th>
                <th>Email</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
            <tr>
                <td><input type="checkbox" name="selected" value="{{ user.userid }}"></td>
                <td>{{ user.userid }}</td>
                <td>{{ user.username }}</td>
                <td>{{ user.name }}</td>
                <td>{{ user.email }}</td>
            </tr>
            {% endfor %}
        </tbody>
        </form>
    </table>
    <button >删除</button>
    </form>
{% endblock %}
